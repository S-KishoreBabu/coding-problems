#include<stdio.h>// not completed
#include<string.h>
#include<strings.h>

int main(){
    char list[]="the time is working good";
    char *new=strchr(list, ' '); 
    printf("\n %s",new);
    for()
    return 0;
}